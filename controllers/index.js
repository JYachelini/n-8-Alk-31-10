const userController = require('./users');
const authController = require('./auth');
const categoriesController = require('./categories');

module.exports = { userController, authController, categoriesController };
